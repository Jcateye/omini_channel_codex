## ADDED Requirements
### Requirement: CRM field mapping management
The system SHALL allow operators to view and update CRM field mapping settings.

#### Scenario: Update mapping in console
- **WHEN** an operator updates the mapping in the console
- **THEN** the system persists the mapping and applies it to inbound CRM updates
