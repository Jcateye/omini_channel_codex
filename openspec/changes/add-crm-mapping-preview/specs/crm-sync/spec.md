## ADDED Requirements
### Requirement: CRM mapping preview
The system SHALL provide a preview of mapped metadata for a given mapping and CRM payload.

#### Scenario: Preview mapping output
- **WHEN** an operator submits a mapping and payload for preview
- **THEN** the system returns the resulting metadata output
