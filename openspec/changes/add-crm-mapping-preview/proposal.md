# Change: Add CRM mapping preview

## Why
Operators need to see how mapping + payload would be applied before saving or sending updates.

## What Changes
- Add API endpoint to preview mapped metadata output
- Extend console UI to show preview results

## Impact
- Affected specs: crm-sync
- Affected code: API, console UI
