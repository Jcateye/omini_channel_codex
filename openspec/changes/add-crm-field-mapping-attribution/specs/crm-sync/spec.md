## ADDED Requirements
### Requirement: CRM metadata field mapping
The system SHALL support CRM field mapping that stores inbound fields into lead metadata.

#### Scenario: Map CRM fields to metadata
- **WHEN** a CRM update includes mapped fields
- **THEN** the fields are stored in lead metadata using configured keys
