# Change: Add CRM mapping examples and validation

## Why
Operators need examples to get started and validation to avoid invalid CRM mapping fields.

## What Changes
- Add mapping examples endpoint and default templates
- Add mapping validation endpoint and server-side checks
- Extend console UI to load examples and validate mapping JSON

## Impact
- Affected specs: crm-sync
- Affected code: API endpoints, console UI
